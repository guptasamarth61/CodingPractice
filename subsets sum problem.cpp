// sum=17
// n=4
// A[]={2,4,6,9}

#include<bits/stdc++.h>
using namespace std;
vector<int> subsets(vector<int> A, int sum){
    sort(A.begin(), A.end());
    int dp[sum+1];
    vector <int> ans;
    
}